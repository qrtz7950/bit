ServletContext는 서블릿들끼리 같은 것들을 공유할수있는 영역이다.

ServletContext는 서버가 시작되는 순간부터 실행되어야 하며 listner에서 그 역할을 하게된다.

@WebListener를 붙이는거로 리스너를 만들수있다

외부에서 갖고온 객체를 자신의 클래스에서 객체로 쓰는것을 의존주입 이라고 한다


최종적으로는 컨트롤러와 리스너에만 dao를 객체를 추가시켜주면 확장이 가능해진다
